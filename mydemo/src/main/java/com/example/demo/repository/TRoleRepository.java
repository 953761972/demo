package com.example.demo.repository;

import com.example.demo.bean.TRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TRoleRepository extends JpaRepository<TRole, Integer> {
}